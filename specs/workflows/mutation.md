# Recursive Epistemic Mutation (Self-Planning)
1. **Signal:** Apoptosis manager detects health degradation (< 85% success).
2. **Erasure:** Perform Sox2 Context Flush to clear 'epigenetic noise' (chat history) [Source 254].
3. **Mutation:** The agent utilizes Gemini CLI to execute `/speckit.plan` autonomously.
4. **Optimization:** Based on the failure logs, the agent rewrites `specs/tasks.md` to implement more rigid PRR filters or slower metabolic velocity via the MX Dial [Source 616, 250].
5. **Metabolism:** The agent settles the 'Re-Planning Fee' (compute cost) via a sub-cent USDC transaction on Arc [Source 903].
6. **Fixation:** The new 'Task-Genome' hash is written to the Arc Block Explorer to create the new Epigenetic Barrier.
