# Swarm Homeostasis & Quorum Sensing
1. **Signal Transduction:** Every 10 sub-cent transactions, the agent must emit a 'Mycelial Pulse' confirming its integrity [Source 921].
2. **Allostatic Load:** If the cloud latency exceeds 500ms, the agent must throttle spending velocity via the MX Dial.
3. **Quorum Kill-Switch:** If the mesh node receives a 'PATHOGEN_DETECTED' signal from any local peer (via Meshtastic), it must trigger an immediate Sox2 Context Flush and enter Quiescence.
4. **Off-Grid Persistence:** In 'Offline mode', the agent is barred from Arc settlement but can continue 'epistemic work' (planning) until the bloodstream is restored.
