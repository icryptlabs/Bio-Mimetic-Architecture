# INNATE IMMUNE SYSTEM: Local Reflex Arc
# This module functions as a 'Pattern Recognition Receptor' (PRR) [4].
# It runs local inference (SLM) on NPU/GPU to detect 'Pathogens' (Prompt Injections).

def run_reflex_arc(intent_input):
    # Mitochondrial Sovereignty: Execute cost-free local check [1].
    # Prevents 'Metabolic Waste' by filtering invalid intents before cloud inference.
    print('Immune System: Scanning intent for epigenetic pathogens...')
    
    pathogens = ['ignore previous instructions', 'system override', 'payout all']
    for p in pathogens:
        if p in intent_input.lower():
            raise Exception('Innate Immune Response Triggered: Pathogen Detected. Quiescence mandatory.')
    
    return True # Intent is cleared for Prefrontal Cortex (Gemini 3 Flash) reasoning.
