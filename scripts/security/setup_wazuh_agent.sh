#!/bin/bash
# Deploy Wazuh unified agent for SFA-One immunity [Source 949, 951]

# Placeholder for installation command via official packages
# curl -s https://packages.wazuh.com/install.sh | bash -s agent

# Authenticate the agent with the security manager
# /var/ossec/bin/agent-auth -m security-manager.sfa-one.io

# Start the protection service
# systemctl enable wazuh-agent && systemctl start wazuh-agent
